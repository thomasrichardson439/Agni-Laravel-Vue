export default {
    classList: {
        type: Array,
        default() {
            return [];
        }
    },
    hasSelect: {
        type: Boolean,
        default: true
    },
    hasEdit: {
        type: Boolean,
        default: true
    },
    hasExpand: {
        type: Boolean,
        default: true
    },
    columns: {
        type: Array,
        default() {
            return [];
        }
    }
};
